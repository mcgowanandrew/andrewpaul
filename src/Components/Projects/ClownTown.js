import React from "react"
import styled from "styled-components";

const ClownTown = ()=>{
    return <BigWrap>hey</BigWrap>;
};
const BigWrap = styled.div`
  width: 100vw;
  height: 100vh;
  margin-left: -20vw;
  padding-left: 20vw;
  background-color: rgba(0, 0, 0, 0.6);
`;

export default ClownTown